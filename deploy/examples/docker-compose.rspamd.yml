services:
  rspamd:
    image: rspamd/rspamd:latest
    container_name: sender-report-rspamd
    restart: unless-stopped
    expose:
      - "11334"
    mem_limit: 256m
    # Controller access + pinned DNS resolvers (8.8.8.8 / 1.1.1.1).
    volumes:
      - ./rspamd/local.d:/etc/rspamd/local.d:ro
